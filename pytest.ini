[pytest]
# Path A: tests_archive/ holds the tests for the archived local-model organs.
# They import modules that are no longer on the active path, so a bare `pytest`
# from the project root must not collect them -- it would report 100 import
# errors for code that was deliberately retired, not broken.
# To run the archive anyway: pytest tests_archive
testpaths = tests
