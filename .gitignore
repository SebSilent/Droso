# Secrets & Configs
.env
credentials.json
*.key

# Local Data/State
.cache/
agent_state/
state/*.npy
state/*.sqlite3
house_stdout.log
lived_time.json

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Temporal files
_tmp/
_verify_*.py
_battery_*.py
_sandbox_run.py

# Local working paper
PAPER.md
PLAN.md

# Derived neural state. Regenerable from books/ via tools/population.py, and far
# too large for git. Lineages on state/lineage/ where a lived individual can be restored without a clone.
state/*.san.npy
state/*.cortex.npz
state/*.cortex.meta.json
state/*.cortex.facts.json
state/*.seq.npz
state/*.seq.slots.json
state/**/*.san.npy
state/**/*.cortex.npz
state/**/*.cortex.meta.json
state/**/*.cortex.facts.json
state/**/*.seq.npz
state/**/*.seq.slots.json
state/**/*.npy
state/**/*.npz
state/**/*.sqlite3
state/reader/state/
state/lineage/
state/population/
state/merged/

# Public-domain books fetched by tools/make_library.py.
# Reproducible exactly from a URL and a script.
books/*.txt

# Runtime artefacts: All regenerable from the corpus, the tools and the clock.
state/merged*/
state/reading/
state/shelf/
state/fitness/
state/population/
state/reader.log
state/lineage/
state/drill_store.json
